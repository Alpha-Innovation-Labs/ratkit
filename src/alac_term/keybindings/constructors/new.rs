use crate::alac_term::keybindings::AlacTermKeyBindings;

impl AlacTermKeyBindings {
    /// Create a new key bindings configuration with default values
    pub fn new() -> Self {
        Self::default()
    }
}
